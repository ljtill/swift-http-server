This is a simple text file for testing the HTTP server.

It contains multiple lines and can be used to verify that plain text files are served correctly with the proper MIME type (text/plain).

Content length: This file should have a proper Content-Length header when served.